func fib(n: i64) -> i64 {
    if n < 2 {
        n
    } else {
        fib(n - 1) + fib(n - 2)
    }
}

func main() -> i32 {
    extern "C" func printf(s: str, ...) -> i32;

    printf("%d\n", fib(40))
}